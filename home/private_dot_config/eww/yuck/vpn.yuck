(defpoll vpn-state :interval "1s" "docker inspect clash -f '{{.State.Running}}'")
(defwidget vpn []
  (eventbox
    :cursor "pointer"
    :tooltip "VPN"
    :class "VPN"
    (button
      :class "VPN-${vpn-state}"
      :valign "fill"
      :onclick "docker start clash"
      " "
    )
  )
)
